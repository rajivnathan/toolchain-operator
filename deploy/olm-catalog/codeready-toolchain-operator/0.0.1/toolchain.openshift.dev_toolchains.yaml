apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  creationTimestamp: null
  name: toolchains.toolchain.openshift.dev
spec:
  additionalPrinterColumns:
  - JSONPath: .status.conditions[?(@.type=="ToolchainReady")].status
    name: Ready
    type: string
  - JSONPath: .status.conditions[?(@.type=="ToolchainReady")].reason
    name: Reason
    type: string
  group: toolchain.openshift.dev
  names:
    kind: Toolchain
    listKind: ToolchainList
    plural: toolchains
    singular: toolchain
  scope: Cluster
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: Toolchain defines which operators should be installed and how
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: ToolchainSpec defines the desired state of Toolchain
          properties:
            components:
              items:
                type: object
              type: array
          required:
          - components
          type: object
        status:
          description: ToolchainStatus defines the observed state of Toolchain
          properties:
            conditions:
              description: 'Last known condition of the OpenShift Pipelines operator
                installation. Supported condition types: ToolchainReady'
              items:
                properties:
                  lastTransitionTime:
                    description: Last time the condition transit from one status to
                      another.
                    format: date-time
                    type: string
                  message:
                    description: Human readable message indicating details about last
                      transition.
                    type: string
                  reason:
                    description: (brief) reason for the condition's last transition.
                    type: string
                  status:
                    description: Status of the condition, one of True, False, Unknown.
                    type: string
                  type:
                    description: Type of condition
                    type: string
                required:
                - status
                - type
                type: object
              type: array
          type: object
      type: object
      x-kubernetes-preserve-unknown-fields: true
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
